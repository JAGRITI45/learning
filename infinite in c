#include <stdio.h>

int main() {
    // Infinite loop
    while (1) {
        printf("This loop will run forever. Press Ctrl+C to stop it.\n");
    }

    return 0; // This line will never be reached
}
